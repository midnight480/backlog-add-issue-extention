# サプライチェーン攻撃対策
# 公開から 3 日経過していないバージョンはインストール対象から除外する。
# （悪意あるハイジャック版は通常数時間〜1日以内に検出・削除されるため、
#  数日間の隔離期間を設けるだけで多くの被害を防げる）
#
# npm v11.10.0 以降で利用可能な設定。単位は「日数」（整数）。
# 参考: https://docs.npmjs.com/cli/v11/using-npm/config#min-release-age
min-release-age=3
